export interface IComponentWithChildren {
    children: React.ReactNode;
};

export interface IUserDetails {
    id: number;
    email: string;
    firstname: string;
    lastname: string;
    avatar: string;
};